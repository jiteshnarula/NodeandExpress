block content
    h1 #{title}
    p Employee  management System
    table(border='1')
        tr
            th Id
            th Name
            th Department
            th Salary
            th(colspan=2) Action
        tr    
         each data ,i in empData
            tr
                td=data.empid
                td=data.empname
                td=data.empdept
                td=data.empsalary
                td
                 a(href='/searchemp/' + data._id) UPDATE
                td
                 a(href='/deleteemp/' + data._id) Delete
    a(href='/add') Add Employee